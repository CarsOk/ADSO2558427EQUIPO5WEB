<div class="row">
  <div class="col-lg-12">
    <div class="ibox-title">
      <div class="ibox product-detail">
        <div class="ibox-content">
          <div class="ibox-tools">
            <a href="<%= edit_cancha_path(@cancha) %>" class="btn btn-white btn-sm"><i class="fa fa-cogs"></i></a>
            <a href="<%= calendario_canchas_path(@cancha) %>" class="btn btn-white btn-sm"><i class="fa fa-calendar"></i></a>
          </div>
          <div class="row">
            <div class="col-md-5">
              <div class="product-images">
                <div>
                  <div class="image-imitation">
                    <%= image_tag @cancha.image.url if @cancha.image.present? %>
                  </div>
                </div>
              </div>
            </div>
            <div class="col-md-7">
              <h2 class="font-bold m-b-xs">
                Cancha N°<%= @cancha.id %>
              </h2>
              <span style="font-size: 20px;"><%= @cancha.nombre %></span>
              <hr>
              <div>
                <a href="<%= new_reserva_path %>" class="btn btn-primary float-right">Reservar</a>
                <h1 class="product-main-price">$<%= @cancha.precio.to_i %></h1>
                <small class="text-muted">Fin de semana: $<%= @cancha.precio_finde.to_i %></small>
              </div>
              <hr>
              <h4>Descripción:</h4>
              <dl class="row m-t-md small">
                <dt>Cantidad de jugadores:</dt>
                <dd>La cantidad de jugadores requerida para nuestra cancha es de <%= @cancha.jugadores %>.</dd>
                <dt>Distancia:</dt>
                <dd>La distancia de esta cancha es de <%= @cancha.distancia %>, para mejor comodidad de nuestros clientes.</dd>
              </dl>
                <div class="text-right">
                  <div class="btn-group">
                    <%= link_to '<i class="fa fa-mail-reply"></i> Regresar'.html_safe, :back, class: 'btn btn-white btn-sm' %>
                    <a href="#" target="_blank" class="btn btn-white btn-sm"><i class="fa fa-comments"></i> Comentarios </a>
                    <%= button_to "Eliminar", @cancha, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger btn-xs float-right ml-2" %>
                  </div>
                </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>



